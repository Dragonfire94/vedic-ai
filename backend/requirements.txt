fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
requests==2.31.0
pyswisseph==2.10.3.2
pytz==2024.1
reportlab==4.0.9
openai==1.12.0
pydantic==2.6.0
```

3. **다른 이름으로 저장**:
   - 위치: `C:\dev\vedic-ai\backend\`
   - 파일명: `requirements.txt`

---

### Step 3: GitHub에 푸시

**GitHub Desktop** 열기:

1. 왼쪽에 **2개 변경사항** 보일 거예요:
   - `backend/Dockerfile`
   - `backend/requirements.txt`

2. 왼쪽 아래:
   - Summary: `Add Dockerfile and requirements for Railway`
   
3. **"Commit to main"** 클릭

4. **"Push origin"** 클릭 (위쪽에 버튼 나타남)

---

### Step 4: Railway 재배포

Railway로 돌아가서:

1. 왼쪽 **"vedic-ai"** 프로젝트 클릭
2. **"Deployments"** 탭 클릭
3. 자동으로 새 배포 시작됨 (GitHub push 감지)
4. 또는 오른쪽 위 **"Redeploy"** 클릭

**3-5분 기다리기** → 빌드 로그 확인

---

## 🔍 빌드 성공 확인

로그에서 이런 메시지 나오면 성공:
```
✓ Build successful
✓ Deployment live