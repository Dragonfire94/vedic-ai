# PATCH: v1.1.4 → v1.1.5

> **적용 방법**: `PRODUCT_SPEC_PRD_v1_1_4.md` 위에 이 패치를 적용합니다.
> 이 패치가 명시하지 않은 모든 내용은 v1.1.4를 따릅니다.

---

## 문서 헤더 교체

```
# PRODUCT_SPEC_PRD v1.1.5 — 상업용 베딕 리포트 엔진 (Contract-Complete)
- **Last updated (Asia/Seoul):** 2026-03-02
```

---

## 변경 로그 — 최상단에 아래 블록 삽입

```markdown
### v1.1.5 *(현재)*

#### [대체] 제품 1 — 인생 흐름 리포트 → 인생 주기 리포트 (Life Cycle Map)
- **v1.1.4 제품**: "90일 실행 전략 중심" 단기 리포트.
- **v1.1.5 전환 이유**: 90일 단기 예측은 재구매 유인이 약하고 다른 리포트(신년운세)와
  차별화가 어려움. 베딕 엔진의 핵심 강점인 Vimshottari Dasha 전주기(120년) 계산값을
  활용해 "내 인생의 큰 파도" 전체 지도를 보여주는 상위 상품으로 재설계.
- **구조 전략**: 인생 전체 70% + 다음 3년 구체화 30% → 3년 후 업데이트 리포트로 재구매 연결.
- **변경 범위**: 섹션 3.2(valid_until), 3.4(제품 목록), 7.1(전체 재작성).
  게이트/패턴/HF 계약은 변경 없음.

#### [신규] 행성 다샤 → 소비자 주제 라벨 변환 테이블 추가 (섹션 7.1.3)
- 9개 행성(Graha)별 소비자 노출 주제 라벨, 핵심 키워드, 경계/기회 톤을 결정론으로 고정.
- 엔진이 다샤 주인 행성 코드를 출력하면 이 테이블로만 변환. 임의 해석 금지.

#### [신규] 인생 단계 그룹핑 알고리즘 정의 (섹션 7.1.4)
- 마하다샤 sequence를 "4단계" 서사로 변환하는 규칙을 결정론으로 고정.

#### [신규] 예언 리스크 방어 문체 규칙 추가 (섹션 7.1.5)
- 인생 주기 리포트 전용 금지 표현 목록 + 권장 대체 표현 고정.
  ("이 시기에 반드시 이런 일이 일어납니다" 등 단정 표현 차단)

#### [수정] 섹션 7.3 CTA A — 제품명 참조 업데이트
- "인생 흐름 리포트" → "인생 주기 리포트" 참조 교체.
```

---

## 섹션 3.2 — valid_until (해당 행 교체)

**v1.1.4 원문:**
> | 인생 흐름 리포트 | `as_of_local + 90일` | "유효기간: 발행 기준 90일" |

**교체 후:**
```markdown
| 인생 주기 리포트 | `min(as_of_local + 3년, 다음 마하다샤 전환일)` | "유효기간: 발행 기준 3년 (또는 다음 인생 주기 전환 시점)" |
```

> **근거**: 마하다샤 평균 주기(7~20년)에 비해 3년은 충분히 짧아 "3년 후 업데이트 리포트"
> CTA로 재구매를 자연스럽게 연결할 수 있음.
> `다음 마하다샤 전환일`이 3년 이내이면 그 날짜를 valid_until로 사용해
> "당신의 인생 주기가 곧 전환됩니다" 알림 CTA를 더 강하게 만들 수 있음.
> 둘 중 **먼저 도래하는 날**을 valid_until로 사용.

---

## 섹션 3.4 — 제품 목록 (1번 항목 교체)

**v1.1.4 원문:**
> 1. **인생 흐름 리포트**: 인생 주기/큰 시즌 설명 + 다음 90일 실행 전략

**교체 후:**
```markdown
1. **인생 주기 리포트 (Life Cycle Map)**: 마하다샤 기반 인생 전체 주기 지도 +
   고점/저점/전환점 + 반복 패턴 분석 + 다음 3년 구체화
```

---

## 섹션 7.1 — 인생 주기 리포트 (Life Cycle Map) (전체 교체)

**v1.1.4 원문 `### 7.1 인생 흐름 리포트`부터 `### 7.2` 직전까지 전체를 아래로 교체:**

```markdown
### 7.1 인생 주기 리포트 (Life Cycle Map)

#### 7.1.1 목적/핵심 가치

- **핵심 질문**: "내 인생의 큰 파도는 언제 오고, 언제 내려가는가?"
- **엔진 기반**: Vimshottari Dasha 120년 주기 전체 계산값 활용.
  다른 어떤 상품보다 "오직 나만을 위한 설계도" 체감이 강함.
- **상업적 목표**: 단기 예측 상품이 줄 수 없는 "인생 서사 전체"를 제공해
  프리미엄 가격대 포지셔닝 + 3년 주기 업데이트 리포트로 재구매 연결.
- **구조 전략 (LOCK)**: 인생 전체 70% + 다음 3년 구체화 30%.
  전체만 있으면 재구매 유인이 없고, 구체화만 있으면 신년운세와 차별화 불가.

#### 7.1.2 출력 구조 (LOCK)

| # | 섹션 | 엔진 입력 | 분량 캡 |
|---|---|---|---|
| 0 | 커버 | 이름, 출생정보, 적용 기간(출생~80세) | 1페이지 |
| 1 | How to use 1p | — | 1페이지 |
| 2 | 인생 구조 한 장 요약 | 4단계 구조 + 현재 위치 | 1페이지 |
| 3 | 마하다샤 인생 단계 목록 | 전체 다샤 날짜 + 주제 라벨 | 행당 4~6줄, 총 9행 이내 |
| 4 | 인생 고점/저점 지도 | pressure_score 기반 상위/하위 구간 | 상승 3 + 전환점 5 + 경계 3 |
| 5 | 반복 패턴 분석 | 행성 도메인별 반복 구간 | 3개 영역 × 최대 4줄 |
| 6 | 다음 3년 구체화 | 현재 부크티 전환 일정 | 날짜 구간 A/B 기준, 최대 5슬롯 |
| 7 | 방법론 카드 6줄 + 면책 | — | HF15 자동 검증 |
| 8 | CTA | — | 7.1.5 규칙 |

> **분량 캡 이유**: "너무 길어질 위험" 방어. 각 섹션이 캡을 초과하면
> 편집 단계에서 압축. 캡 초과 자체는 HARD FAIL 대상이 아니나
> 수동 QA 필수 항목으로 지정.

#### 7.1.3 행성 다샤 → 소비자 주제 라벨 변환 테이블 (LOCK)

> 엔진이 출력하는 다샤 주인 행성(Graha) 코드를 소비자 본문 주제 라벨로 변환할 때
> **반드시 이 테이블만 사용**합니다. 구현자 임의 해석 금지.

| 행성 코드 | 한국어명 | 소비자 주제 라벨 | 핵심 키워드 | 기본 톤 |
|---|---|---|---|---|
| `SU` | 태양(Surya) | 자아 확립·리더십 | 책임, 인정, 중심 | 상승 |
| `MO` | 달(Chandra) | 감정·관계·직관 | 돌봄, 유동, 가정 | 중립 |
| `MA` | 화성(Mangal) | 에너지·도전·행동 | 추진, 충돌, 개척 | 혼합 |
| `RA` | 라후(Rahu) | 확장·혼돈·야망 | 새 환경, 불확실, 성장 | 혼합 |
| `JU` | 목성(Guru) | 성장·지혜·풍요 | 확장, 배움, 기회 | 상승 |
| `SA` | 토성(Shani) | 압축·카르마·책임 | 느림, 인내, 결실 | 경계 |
| `ME` | 수성(Budha) | 소통·분석·학습 | 커뮤니케이션, 지식, 변화 | 중립 |
| `KE` | 케투(Ketu) | 내면·해방·영성 | 과거 청산, 직관, 고독 | 경계 |
| `VE` | 금성(Shukra) | 관계·창의·물질 | 인간관계, 예술, 안락 | 상승 |

**기본 톤 사용 규칙 (LOCK)**:
- `상승`: 기회/성장 프레임으로 서술 → 행동은 "확장" 계열
- `경계`: 주의/인내 프레임으로 서술 → 행동은 "보호/점검" 계열
- `중립`: 해석 맥락(앞뒤 다샤)에 따라 결정 → 소비자 상황에 맞게 분기
- **기본 톤이 `경계`여도 부정 결론 단독 제시 금지** (0.5.4 원칙 유지).
  반드시 "이 구간의 에너지를 활용하는 방법" 대안 행동 1개 병기.

#### 7.1.4 인생 단계 그룹핑 알고리즘 (결정론, LOCK)

> 마하다샤 sequence를 소비자에게 "4단계 인생 서사"로 보여주기 위한 규칙.

**입력**: 출생부터 80세까지의 마하다샤 목록 (시작일, 종료일, 행성 코드)

**알고리즘 (LOCK)**:

```python
# commercial_gate_helpers.py (참고 — 엔진 구현부에서 활용)

LIFE_STAGE_LABELS = ["기반 형성", "방향 탐색", "사회적 확장", "영향력 축적"]
# 4단계 라벨 변경은 PRD 버전업으로만 허용

def assign_life_stages(dashas: list[dict], birth_year: int) -> list[dict]:
    """
    마하다샤 목록을 4단계로 그룹핑.
    각 그룹의 경계는 연령 4분위가 아니라 다샤 전환점 기준.

    규칙 (LOCK):
    1. 총 마하다샤 수 N을 4로 나눠 그룹 경계 인덱스 산출 (정수 나눗셈).
       예: N=9이면 그룹 크기 = [2, 2, 2, 3]
    2. 각 그룹의 대표 라벨은 LIFE_STAGE_LABELS[i] (0-based).
    3. 각 그룹의 기간 = 첫 번째 다샤 시작일 ~ 마지막 다샤 종료일.
    4. 그룹 내 지배적 행성(기간 합산 최장)을 그룹 대표 행성으로 선정.
    5. 현재 날짜가 속한 그룹에 "현재 위치" 마킹.
    """
    n = len(dashas)
    base, rem = divmod(n, 4)
    sizes = [base + (1 if i < rem else 0) for i in range(4)]

    stages = []
    idx = 0
    for stage_i, size in enumerate(sizes):
        group = dashas[idx: idx + size]
        if not group:
            break
        dominant = max(group, key=lambda d: d["duration_days"])["planet"]
        stages.append({
            "label": LIFE_STAGE_LABELS[stage_i],
            "start": group[0]["start"],
            "end":   group[-1]["end"],
            "dominant_planet": dominant,
            "dashas": group,
        })
        idx += size
    return stages
```

**소비자 한 장 요약 출력 포맷 (고정)**:
```
당신의 인생은 "[그룹0 라벨] → [그룹1 라벨] → [그룹2 라벨] → [그룹3 라벨]" 구조로 설계되어 있습니다.

• [그룹0 시작 연도]~[그룹0 종료 연도]: [그룹0 라벨] — [그룹0 대표 행성 소비자 주제 라벨]
• [그룹1 시작 연도]~[그룹1 종료 연도]: [그룹1 라벨] — [그룹1 대표 행성 소비자 주제 라벨]
• [그룹2 시작 연도]~[그룹2 종료 연도]: [그룹2 라벨] — [그룹2 대표 행성 소비자 주제 라벨]
• [그룹3 시작 연도]~[그룹3 종료 연도]: [그룹3 라벨] — [그룹3 대표 행성 소비자 주제 라벨]

👉 현재 당신은 [현재 그룹 라벨] 구간에 있습니다.
```

#### 7.1.5 고점/저점 지도 산출 기준 (결정론, LOCK)

**입력**: 마하다샤별 `pressure_score` (기존 엔진 출력값)

```python
def compute_life_highs_lows(dashas_with_score: list[dict]) -> dict:
    """
    pressure_score 기준으로 상승/경계/전환점 구간 선정.

    선정 규칙 (LOCK):
    - 상승 구간 3개: pressure_score 상위 3 (동률이면 더 이른 구간 우선)
    - 경계 구간 3개: pressure_score 하위 3
    - 전환점 5개: 인접 마하다샤 간 pressure_score 차이 절댓값 상위 5
      (변화 폭이 클수록 "인생 전환점"으로 정의)
    - 상승 구간과 경계 구간은 서로 중복 불가 (상위 3과 하위 3은 자연 분리)
    """
    sorted_by_score = sorted(dashas_with_score, key=lambda d: d["pressure_score"])
    lows  = sorted_by_score[:3]
    highs = sorted_by_score[-3:]

    transitions = []
    for i in range(1, len(dashas_with_score)):
        delta = abs(dashas_with_score[i]["pressure_score"]
                    - dashas_with_score[i-1]["pressure_score"])
        transitions.append({"date": dashas_with_score[i]["start"], "delta": delta,
                             "from": dashas_with_score[i-1]["planet"],
                             "to":   dashas_with_score[i]["planet"]})
    top_transitions = sorted(transitions, key=lambda t: t["delta"], reverse=True)[:5]

    return {"highs": highs, "lows": lows, "transitions": top_transitions}
```

**소비자 출력 포맷 (고정)**:
```
🔺 가장 상승 가능성 높은 3구간
  1. YYYY-MM-DD ~ YYYY-MM-DD — [행성 주제 라벨] / 기회 창
  ...

⚡ 인생 전환점 5개
  1. YYYY-MM-DD — [이전 행성 주제] → [이후 행성 주제] / 전환 강도: [상/중/하]
  ...

⚠ 경계해야 할 구간 3개
  1. YYYY-MM-DD ~ YYYY-MM-DD — [행성 주제 라벨] / 주의 창 + 대응 행동 1개
  ...
```

> **전환 강도 라벨 기준 (LOCK)**:
> - delta 상위 33% → "상" / 중간 33% → "중" / 하위 33% → "하"

#### 7.1.6 반복 패턴 분석 기준 (결정론, LOCK)

**입력**: 마하다샤 목록 + 행성 도메인 분류

```python
# commercial_quality_constants.py

PLANET_DOMAIN_MAP: dict[str, list[str]] = {
    "관계":   ["VE", "MO"],           # 금성, 달
    "돈·커리어": ["JU", "SU", "ME"],  # 목성, 태양, 수성
    "건강·에너지": ["MA", "RA", "KE"], # 화성, 라후, 케투
}
```

```python
def compute_repeat_patterns(dashas: list[dict]) -> dict:
    """
    도메인별 반복 구간 리스트 산출.
    같은 도메인 행성이 2회 이상 등장하면 "반복 패턴"으로 정의.
    """
    patterns = {}
    for domain, planets in PLANET_DOMAIN_MAP.items():
        occurrences = [d for d in dashas if d["planet"] in planets]
        if len(occurrences) >= 2:
            patterns[domain] = occurrences
    return patterns  # 도메인별 발생 다샤 리스트
```

**소비자 출력 포맷 (고정, 도메인당 최대 4줄)**:
```
[도메인명] 반복 시기
  • YYYY ~ YYYY ([행성 주제 라벨]) — 왜 이 시기에 반복되는가 1줄
  • YYYY ~ YYYY ([행성 주제 라벨])
  👉 이 패턴에서 벗어나기 위한 행동: [행동 1개]
```

#### 7.1.7 다음 3년 구체화 섹션 (재구매 훅, LOCK)

**위치**: 섹션 5번 (반복 패턴) 직후. 전체 리포트에서 **마지막 본문 섹션**.

**목적**: 인생 전체(70%) 이후 "그래서 지금 당장 무엇을 해야 하는가"(30%)로 연결.
  재구매 없이도 현재 구체 행동을 줄 수 있어야 하지만,
  3년이 지나거나 주요 부크티가 전환되면 "업데이트 리포트"가 자연스럽게 필요해지는 구조.

**엔진 입력**: 현재 마하다샤 + 향후 3년 이내 부크티 전환 일정

**출력 규칙 (LOCK)**:
- 향후 3년 이내 부크티 전환점을 날짜 구간 A 기준으로 최대 5슬롯 출력.
- 각 슬롯: `YYYY-MM-DD~YYYY-MM-DD` + 부크티 주인 행성 주제 라벨 + 행동 1개.
- 슬롯이 3년 이내 5개 미만이면 있는 만큼만 출력 (패딩 금지).
- 마지막 슬롯 이후 반드시 아래 고정 문구 삽입:

```
이 구간이 지나면 당신의 인생 주기 지도는 새로운 챕터로 넘어갑니다.
3년 후 또는 다음 주요 전환점에서 업데이트된 지도를 확인해보세요.
```

#### 7.1.8 예언 리스크 방어 문체 규칙 (LOCK)

> 인생 주기 리포트는 장기 예측 특성상 "예언처럼 보이는 위험"이 다른 제품보다 높습니다.
> 아래 금지/권장 규칙은 기존 0.5.1 문체 규칙에 **추가**로 적용됩니다.

**금지 표현 (이 제품 전용 추가 목록):**

| 금지 표현 패턴 | 이유 |
|---|---|
| "이 시기에 반드시 ~이 일어납니다" | 단정 예언 → 법적/윤리 리스크 |
| "~살이 되면 ~를 경험합니다" | 확정적 미래 기술 |
| "이 구간에서 큰돈을 벌 수 있습니다" | 투자 조언 유사 |
| "이 시기에 결혼/이별이 옵니다" | 관계 확정 예언 |
| "~다샤가 끝나면 모든 게 해결됩니다" | 근거 없는 보장 |

**권장 대체 표현:**

| 금지 | 권장 |
|---|---|
| "반드시 일어납니다" | "가능성이 높아집니다 / 이 에너지가 활성화됩니다" |
| "큰돈을 법니다" | "물질적 확장의 기회가 열리는 구간입니다" |
| "결혼이 옵니다" | "관계가 깊어지거나 새로운 연결이 생기기 쉬운 구간입니다" |
| "끝나면 해결됩니다" | "이 구간 이후 새로운 방향성이 열릴 가능성이 큽니다" |

**게이트 자동 차단**: 기존 HF5 (`dasha_definition_redundancy_violations`) 외에도
이 제품에서는 HF11 공감 첫 문장 체크가 더 엄격하게 적용됩니다 (추가 게이트 신설은 하지 않되,
수동 QA에서 예언 리스크 문장을 필수 점검 항목으로 추가).

#### 7.1.9 CTA/업셀 규칙 (LOCK)

- **위치**: 섹션 8(CTA) 1개 + 다음 3년 구체화 섹션 끝 고정 문구 (7.1.7)
- **업셀 트리거 (결정론)**:
  - `onboarding_goal == "relationship"` 또는 ⑤ 관계 상태 입력 있음 → 궁합 리포트 CTA
  - `valid_until`이 다음 마하다샤 전환일 기준인 경우 → "전환점 심화 리포트" CTA 우선
  - 그 외 → 신년운세 리포트 CTA
- **문구 포맷 (고정)**: "[현재 인생 단계 라벨]에서 [다음 단계 라벨]로 넘어가는 당신에게,
  [다음 상품명]이 더 구체적인 방향을 드릴 수 있습니다."
- **버튼 텍스트**: 20자 이내. 예: "신년운세 리포트 보기", "궁합 리포트 확인하기",
  "3년 업데이트 리포트 예약하기"
- **유효기간 만료 알림**: CTA 하단에 "다음 인생 주기 전환점 알림을 받으시겠습니까?" 동의 버튼 병기
  (valid_until 기준 30일 전 알림 — 프론트 연동)
```

---

## 섹션 7.3.2 CTA A — 참조 업데이트

**v1.1.4 원문:**
> - **CTA A (개인 업셀)**: 두 사람 각각의 이름(①토큰)으로 인생 흐름 리포트 CTA

**교체 후:**
```markdown
- **CTA A (개인 업셀)**: 두 사람 각각의 이름(①토큰)으로 인생 주기 리포트 CTA.
  예: "[A이름]의 인생 주기 리포트 보기" + "[B이름]의 인생 주기 리포트 보기"
```

---

## 섹션 9.4 — 패턴/정규식 정의 (항목 추가)

**`commercial_quality_constants.py` 블록 끝에 아래 추가:**

```python
# ── 행성 도메인 분류 (LOCK, 반복 패턴 분석용) ────────────────────────────────
PLANET_DOMAIN_MAP: dict[str, list[str]] = {
    "관계":       ["VE", "MO"],
    "돈·커리어":  ["JU", "SU", "ME"],
    "건강·에너지": ["MA", "RA", "KE"],
}

# ── 인생 단계 라벨 (LOCK) ─────────────────────────────────────────────────────
LIFE_STAGE_LABELS: list[str] = ["기반 형성", "방향 탐색", "사회적 확장", "영향력 축적"]

# ── 전환 강도 분위 임계치 (LOCK) ─────────────────────────────────────────────
# delta 상위 33% → "상" / 중간 33% → "중" / 하위 33% → "하"
TRANSITION_INTENSITY_THRESHOLDS = (0.33, 0.67)  # 하위/상위 분위 경계
```

---

## 섹션 10.1 — 자동 테스트 추가 항목

**기존 테스트 목록 끝에 아래 추가:**

```markdown
**인생 주기 리포트 전용 테스트 (v1.1.5 신규)**:
- `assign_life_stages()`: 마하다샤 9개 → 그룹 크기 [2,2,2,3] 정확히 분할
- `assign_life_stages()`: 마하다샤 4개 → 그룹 크기 [1,1,1,1]
- `compute_life_highs_lows()`: 상위 3 / 하위 3 / 전환점 5 정확히 선정
- `compute_life_highs_lows()`: 다샤가 5개일 때 전환점이 4개(N-1)만 생성되는지 확인
- `compute_repeat_patterns()`: 동일 도메인 행성 2회 이상 등장 시 패턴 인식
- `compute_repeat_patterns()`: 동일 도메인 행성 1회만 등장 시 패턴 제외 확인
- 행성 주제 라벨 변환: 9개 행성 코드 각각 → `PLANET_LABEL_MAP` 정확 변환 (섹션 7.1.3 테이블 기준)
- valid_until: `as_of_local + 3년` vs `다음 마하다샤 전환일` → 더 이른 날짜 선택 확인
- 다음 3년 구체화 슬롯: 3년 이내 부크티 전환이 3개일 때 3개만 출력 (패딩 없음)
- 예언 리스크 문체 수동 QA 체크리스트: 금지 표현 패턴 8개 항목 육안 확인

**수동 QA 추가 항목 (v1.1.5)**:
- 각 마하다샤 섹션 분량이 4~6줄 캡 이내인지 확인
- 인생 고점/저점 지도의 경계 구간에 대안 행동이 있는지 확인
- 예언 리스크 금지 표현 없는지 확인
- "다음 3년 구체화" 섹션 끝에 업데이트 유도 고정 문구 존재 여부 확인
```

---

## 섹션 3.1 — 메타 필드 (PLANET_LABEL_MAP 참조 추가)

**`onboarding_goal` 항목 아래에 아래 행 추가:**

```markdown
| `current_mahadasha_planet` | 행성 코드 (2자) | 현재 마하다샤 주인 행성. 인생 주기 리포트 전용. |
| `next_mahadasha_date` | ISO-8601 date | 다음 마하다샤 전환일. valid_until 계산에 사용. |
```

---

## 로드맵 — 해당 행 교체

```markdown
| v1.1.4 | 정규화 함수 책임 분리 / 조사 허용 경계 regex / 분모 공식 명시 / lru_cache |
| **v1.1.5 (현재)** | **인생 주기 리포트(Life Cycle Map) 제품 재설계 — 행성 주제 라벨 / 인생 단계 그룹핑 / 고점저점 / 반복 패턴 / 예언 리스크 방어** |
```

---

## 섹션 14 — 실행 우선순위 (v1.1.5 버전으로 교체)

```markdown
## 14. v1.1.5 실행 우선순위 (Gap-to-Implementation)

> 목적: 인생 주기 리포트(Life Cycle Map) 제품을 엔진 출력값 기반으로
> 결정론적으로 생성할 수 있도록 필요한 계약과 알고리즘을 추가한다.
> 기존 HF 게이트/패턴 계약은 변경 없음.

### 14.1 P0 — 행성 주제 라벨 변환 테이블 상수 추가

1. `commercial_quality_constants.py`에 `PLANET_DOMAIN_MAP`, `LIFE_STAGE_LABELS`,
   `TRANSITION_INTENSITY_THRESHOLDS` 추가.
2. 엔진 출력 행성 코드 → 소비자 주제 라벨 변환 함수를 `commercial_gate_helpers.py`에 추가.
3. 단위 테스트: 9개 행성 코드 → 라벨 변환 정확도 확인.

### 14.2 P0 — 인생 단계 그룹핑 알고리즘 구현

1. `assign_life_stages()` 함수 구현 (섹션 7.1.4 코드 참조).
2. 단위 테스트: 마하다샤 4개/9개/12개 케이스 그룹 분할 검증.

### 14.3 P0 — 고점/저점 지도 산출 구현

1. `compute_life_highs_lows()` 함수 구현 (섹션 7.1.5 코드 참조).
2. 단위 테스트: 상위3/하위3/전환점5 정확 선정 + 다샤 5개 케이스(전환점 4개) 확인.

### 14.4 P1 — 반복 패턴 분석 구현

1. `compute_repeat_patterns()` 함수 구현 (섹션 7.1.6 코드 참조).
2. 단위 테스트: 동일 도메인 2회 이상 등장 → 패턴 인식 / 1회 → 제외 확인.

### 14.5 P1 — valid_until 로직 업데이트

1. 인생 주기 리포트 `valid_until = min(as_of_local + 3년, next_mahadasha_date)` 구현.
2. `next_mahadasha_date` 메타 필드 파이프라인 주입.
3. 단위 테스트: 두 날짜 중 더 이른 날짜 선택 확인.

### 14.6 P1 — 다음 3년 구체화 슬롯 생성

1. 향후 3년 이내 부크티 전환점을 날짜 구간 A 기준으로 최대 5슬롯 출력.
2. 슬롯 수가 5개 미만이면 있는 만큼만 출력 (패딩 금지) 로직 구현.
3. 7.1.7 고정 문구 자동 삽입.

### 14.7 검증/수용 기준

1. 기존 HF 16개 계약 회귀 없음.
2. 인생 단계 그룹핑 단위 테스트 전부 통과.
3. 고점/저점 지도 산출 단위 테스트 전부 통과.
4. 행성 주제 라벨 9개 전부 정확 변환.
5. valid_until `min()` 로직 정확 동작.
6. 수동 QA: 예언 리스크 금지 표현 0건.

### 14.8 비범위 (재확인)

- 엔진 산식(다샤/트랜짓/점수) 변경 금지.
- BTR ON 전환 금지.
- 프론트엔드/결제/로그인/대시보드 비범위.
- LLM 추가 호출 금지.
```

---

*패치 끝. 이 문서에서 명시하지 않은 모든 내용은 `PRODUCT_SPEC_PRD_v1_1_4.md`를 따릅니다.*
